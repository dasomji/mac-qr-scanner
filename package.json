{
  "name": "mac-qr-scanner",
  "title": "Webcam QR Scanner",
  "description": "Scan QR codes from the real world using your Mac's webcam",
  "icon": "command-icon.png",
  "author": "daniel",
  "license": "MIT",
  "commands": [
    {
      "name": "scan-qr-code",
      "title": "Scan QR Code",
      "subtitle": "Webcam QR Scanner",
      "description": "Capture a frame from your webcam and decode any QR code in it",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.93.2",
    "jimp": "^1.6.0",
    "jsqr": "^1.4.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "~22.13.4",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "build-swift": "swiftc swift/CaptureFrame.swift -O -target arm64-apple-macosx12.0 -framework AVFoundation -framework CoreMedia -framework CoreVideo -framework CoreImage -framework ImageIO -o assets/capture-frame-arm64 && swiftc swift/CaptureFrame.swift -O -target x86_64-apple-macosx12.0 -framework AVFoundation -framework CoreMedia -framework CoreVideo -framework CoreImage -framework ImageIO -o assets/capture-frame-x86_64 && lipo -create -output assets/capture-frame assets/capture-frame-arm64 assets/capture-frame-x86_64 && rm assets/capture-frame-arm64 assets/capture-frame-x86_64",
    "build": "npm run build-swift && ray build",
    "dev": "npm run build-swift && ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint"
  }
}
